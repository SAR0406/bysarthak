
{
  "entities": {
    "Conversation": {
      "title": "Conversation",
      "type": "object",
      "description": "Represents a single conversation thread with a visitor.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the conversation."
        },
        "senderName": {
          "type": "string",
          "description": "Name of the visitor who initiated the conversation."
        },
        "senderEmail": {
          "type": "string",
          "description": "Email address of the visitor.",
          "format": "email"
        },
        "lastMessageAt": {
          "type": "string",
          "description": "Timestamp of the last message in the conversation.",
          "format": "date-time"
        },
        "messages": {
          "type": "array",
          "description": "An array of messages within the conversation.",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "description": "Unique identifier for the message."
              },
              "text": {
                "type": "string",
                "description": "The content of the message."
              },
              "imageUrl": {
                "type": "string",
                "description": "URL of an image attached to the message.",
                "format": "uri"
              },
              "sentAt": {
                "type": "string",
                "description": "Timestamp indicating when the message was sent.",
                "format": "date-time"
              },
              "sentBy": {
                "type": "string",
                "description": "Indicates if the message was from the 'visitor' or 'admin'.",
                "enum": ["visitor", "admin"]
              },
              "senderName": {
                "type": "string",
                "description": "Name of the person sending the message."
              },
               "senderEmail": {
                "type": "string",
                "description": "Email of the person sending the message."
              },
              "readBy": {
                "type": "object",
                "description": "Map of user emails to timestamps indicating when they read the message.",
                "additionalProperties": {
                  "type": "string",
                  "format": "date-time"
                }
              }
            },
            "required": ["id", "sentAt", "sentBy", "senderName", "senderEmail"]
          }
        }
      },
      "required": [
        "id",
        "senderName",
        "senderEmail",
        "lastMessageAt"
      ]
    },
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's profile information.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the user profile was created."
        },
        "name": {
          "type": "string",
          "description": "The user's full name."
        },
        "age": {
          "type": "number",
          "description": "The user's age."
        },
        "username": {
          "type": "string",
          "description": "The user's unique username."
        }
      },
      "required": [
        "email",
        "createdAt",
        "name",
        "username",
        "age"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "google.com",
      "github.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/conversations/{conversationId}",
        "definition": {
          "entityName": "Conversation",
          "schema": {
            "$ref": "#/backend/entities/Conversation"
          },
          "description": "Stores conversation threads. Anyone can create a thread. Only authenticated users (admins) can read and update (reply).",
          "params": [
            {
              "name": "conversationId",
              "description": "Unique identifier for the conversation document."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores public-facing user profile information. The document ID is the same as the Firebase Auth UID.",
          "params": [
            {
              "name": "userId",
              "description": "The user's unique ID from Firebase Authentication."
            }
          ]
        }
      }
    ],
    "reasoning": "This design prioritizes security and scalability. A top-level `/conversations` collection stores message threads, identified by the sender's email to group messages. This avoids hierarchical complexities. A separate `/users` collection stores user profiles, linking them by UID to Firebase Authentication. This separation allows for distinct security rules for public user data versus private conversations."
  }
}

    